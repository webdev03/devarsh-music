{
  "name": "devarsh-music",
  "private": true,
  "workspaces": [
    "frontend/",
    "backend/"
  ],
  "scripts": {
    "build": "bun run build --workspaces",
    "dev": "bun run dev --workspaces",
    "start": "bun run start --workspaces",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.6"
  }
}
